from setuptools import find_packages, setup

setup(
    name='cycleformers',
    packages=find_packages(),
    version='0.1.0',
    description='CycleFormers is a library developed to expedite the process of training transformer models using cycle-consistency training.',
    author='William Thorne',
)